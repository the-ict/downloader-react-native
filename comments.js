// 


    // useEffect(() => {
    //     const activeRoute = route.routes[route.index];

    //     const activeStackState = activeRoute.state

    //     const activeStackRoute =
    //         activeStackState && activeStackState.routes
    //             ? activeStackState.routes[activeStackState.index]
    //             : null;

    //     if (activeStackRoute?.name !== "Stack") {
    //         if (currentQueryIndex >= 0) {
    //             if (siteName === "Home-Page") {
    //                 navigation.navigation.navigate("Stack")
    //             } else if (siteName) {
    //                 navigation.navigation.navigate("View", {
    //                     url: siteName
    //                 })
    //             }
// import { useNavigationState, } from "@react-navigation/native"
    
    //         }
    //     }
    // }, [currentQueryIndex])

        // const route = useNavigationState(state => state)
    